# Expense Tracker v2

Adds IndexedDB storage, PIN lock, search, offline support, and AES-GCM encrypted backup export/import.

Upload these files to the root of your existing GitHub repository, replacing the old files. Your transactions remain local to each device. Backup files are created locally and can be saved manually to iCloud Drive, Google Drive, or OneDrive.
